{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buck build",
            "type": "shell",
            "command": "buck build :demo#debug",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}